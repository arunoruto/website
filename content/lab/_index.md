---
title: "Lab"
# Development-only section: its pages are drafts, but Hugo would still emit an
# empty /lab/ list page (and sitemap entry) in production. Never render or list
# the section itself; the draft pages inside are still served by `serve`.
build:
  render: never
  list: never
---
